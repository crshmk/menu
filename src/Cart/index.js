import React, { Component } from 'react'
import { Text } from 'react-native-paper';

export default class Cart extends Component {
  render() {
    return (
      <Text>Let's checkout</Text>
    )
  }
}
